论文标题：基于LLM的智能电商数据分析智能体的实现

## 规划说明

本规划仅依据以下已存在材料展开，不补写不存在的系统能力，也不把“计划功能”写成“已实现功能”：
- `docs/1.需求分析.md`：确认项目目标、基础功能和未实现的拓展项。
- `docs/2.系统整体架构图.md`：确认真实主链路已经收敛为“统一消息入口 + 任务化分析 + 多 Agent 工作流”。
- `docs/4.数据库设计.md`：确认三层存储边界及 MySQL 业务主数据职责。
- `docs/5.API接口定义.md`：确认接口交互协议是“发送消息 -> 轮询进度 -> 拉取结果”的三段式。
- `backend/app/services/analysis_service.py`、`backend/app/agents/workflow.py`：确认任务生命周期、工作流节点、结果落库方式。
- `backend/app/crawlers/jd_crawler.py`：确认评论采集基于 DrissionPage 的真实京东爬虫。
- `frontend-vue/src/main.ts`、`frontend-vue/src/router/index.ts`、`frontend-vue/src/stores/chat.ts`：确认前端当前是 Vue 单页工作台，核心页面为认证页和聊天工作台页。

目录顺序提示：
- 现有目录中“第三章 系统概要设计”在“第四章 系统需求分析”之前，从常见论文写作逻辑看，需求分析通常应先于概要设计。
- 为避免擅自改目录，本规划暂不调整章节顺序，只在现有结构下给出写作安排。
- 若后续你决定正式调整目录，建议仅将第三章与第四章对调，章节内部内容规划基本仍可复用。

写作边界提示：
- “普通 chat 聊天”不能写成当前后端存在一条独立同步回复链路。按现有文档和代码，系统真实实现是统一消息入口，消息进入任务化分析链路。
- “邮箱推送分析报告”在 `docs/1.需求分析.md` 中被标记为未实现，只能写入扩展功能或后续工作。
- “模型测试对比”这一节只能基于真实实验数据撰写；如果后续没有补齐实测数据，不得硬写定量结论。

全文字数规划：
- 目标总字数：约 `16800` 字，满足“大于 1.6 万字”的要求。
- 章节分配：第一章 `2600` 字，第二章 `2600` 字，第三章 `3000` 字，第四章 `2700` 字，第五章 `3500` 字，第六章 `1900` 字，结论 `500` 字。

参考文献使用总原则：
- 全文遵循“单篇文献只引用一次”的规则，避免在前文集中消耗参考文献。
- 引文数量宜少不宜多，优先在最能支撑论点的章节使用，其余内容以系统文档、代码和原创分析为主。
- 当前建议的单次引用分配如下：
  - `[1]`：放在 `1.2 国内外相关研究`，用于 LLM 与 Agentic AI 的总体研究背景。
  - `[8]`：放在 `1.2 国内外相关研究`，用于评论分析从粗粒度情感分析走向细粒度价值提取的研究脉络。
  - `[7]`：放在 `2.1 LangGraph 多智能体编排框架`，用于多 Agent 协作与推理分工的理论支撑。
  - `[4]`：放在 `2.5 DuckDB 内存分析引擎与 ChromaDB 向量数据库`，用于 RAG 与检索增强思路。
  - `[3]`：放在 `3.1 系统整体架构设计`，用于分层式分析 Agent 架构的参考映射。
  - `[2]`：放在 `4.4 可行性分析`，用于说明数据分析智能体路线具有现实可行性。
  - `[10]`：放在 `5.1 数据采集与清洗模块`，用于说明在线评论在电商服务评价中的应用价值。
  - `[5]`：放在 `5.2 多智能体工作流实现`，用于支撑“需求理解到结果总结”的分析型智能体链路。
  - `[9]`：放在 `5.3 数据分析与检索模块`，用于评论语义理解和文本分析模型能力的辅助说明。
  - `[11]`：放在 `6.3 模型测试对比`，用于评价维度、评测原则和测试设计说明。
- `[6]` 当前不建议纳入正文主线引用，除非后续确实需要补充“智能问答系统应用价值”的旁证。

插图与表格使用总原则：
- 只规划当前系统真实存在或可真实生成的图表、流程图、E-R 图、接口时序图和系统截图。
- 图示优先服务于“说明结构、解释流程、支撑测试结果”，避免为了排版而堆图。
- 能用图讲清结构关系的章节优先放图；能用表归纳对比的章节优先放表。
- 所有截图和图表都应来自当前系统实现、当前接口协议或真实运行结果，不使用示意性伪截图。

---

论文结构：

第一章 绪论

【章节规划】
- 建议字数：`2600` 字。
- 章节目标：交代电商评论分析与 LLM 智能体结合的研究背景、相关研究现状、本文的研究问题、系统目标和全文结构。
- 章节基调：先讲电商场景痛点，再讲 Agent 技术机会，最后落到“本系统解决什么问题、采用什么实现路径、论文如何展开”。

1.1 研究背景

【写作规划】
- 建议字数：`700` 字。
- 叙述重点：
  1. 从电商平台商品数量庞大、用户决策成本高、评论信息冗余且非结构化切入，说明“值得买吗”这一问题需要比简单问答更深的证据型分析。
  2. 引出传统评论分析方法存在的不足：难以兼顾评论抓取、结构化统计、语义证据检索和自然语言总结。
  3. 引出 LLM 与 Agent 技术的优势：自然语言理解、任务拆解、工具调用、多步骤推理、结果整合。
  4. 最后落到本文系统场景：面向电商商品评论的智能分析工作台。
- 写作依据：
  - `docs/1.需求分析.md`
  - `docs/2.系统整体架构图.md`
- 本节原则上不单独安排参考文献，背景论述以问题场景和项目动机为主。
- 配图/表格规划：
  - 本节一般不强制放图。
  - 如版面允许，可增加 1 张“电商评论分析问题场景示意图”或 1 个“传统方法与智能体方法差异表”，但必须基于正文论点，不单独扩展新内容。

1.2 国内外相关研究

【写作规划】
- 建议字数：`900` 字。
- 建议分成四个研究脉络写：
  1. LLM 与 Agentic AI 总体研究脉络。
  2. 面向专业领域的数据分析 Agent 与知识服务 Agent 研究。
  3. 多 Agent 协作、知识推理与工具调用研究。
  4. 面向评论文本的情感分析与服务质量分析研究。
- 每个脉络要写“已有成果 + 局限性 + 与本文关系”。
- 对比时应明确：
  - 本文没有采用 MCP，也没有做模型微调，不应把相关研究中的协议化接入路线或领域微调路线写成本文已实现。
  - 本文的特点在于把“电商评论爬取、任务调度、SQL 分析、RAG 检索、图表展示”串成一个统一工作流。
- 引用安排：
  - 如需引用，仅使用上文已分配的“总体研究背景文献”和“评论分析综述文献”。
- 本节结尾要自然推出本文的研究空缺：
  - 现有研究要么偏单领域问答系统，要么偏通用 Agent 框架，要么只做评论情感分析。
  - 缺少一个面向电商商品评论、具有真实采集、任务化调度、多 Agent 协作和前后端闭环的完整实现。
- 配图/表格规划：
  - 本节不再单独配置“国内外研究对比表”。
  - 建议通过段落化综述完成“研究脉络 + 局限性 + 与本文关系”的对比，不额外增加表格编号。

1.3 研究内容及目标

【写作规划】
- 建议字数：`700` 字。
- 叙述重点：
  1. 明确研究对象：基于 LLM 的智能电商数据分析智能体系统。
  2. 明确研究目标：让用户以自然语言和商品链接发起分析任务，系统自动完成评论采集、统计分析、证据检索、结构化结果生成和前端展示。
  3. 明确研究内容应与真实实现一一对应：
     - 统一消息入口与会话机制。
     - 基于 LangGraph 的多 Agent 工作流。
     - MySQL + DuckDB + ChromaDB 三层数据架构。
     - DrissionPage 京东评论爬虫。
     - FastAPI 后端与 Vue 前端工作台。
  4. 明确未实现内容边界：邮件推送仅可写作扩展设想，不写为研究成果。
- 写作依据：
  - `docs/1.需求分析.md`
  - `docs/2.系统整体架构图.md`
  - `docs/5.API接口定义.md`
- 配图/表格规划：
  - 可放 1 个“本文研究内容与系统模块对应表”。
  - 表头建议包含：研究内容、对应文档/代码位置、实现状态、论文落章位置。

1.4 论文章节安排

【写作规划】
- 建议字数：`300` 字。
- 叙述方式：
  - 逐章概括，不展开技术细节。
  - 注意说明第三章和第四章当前目录顺序沿用现稿结构。
- 写法要求：每章用 2-3 句话概括“回答什么问题”。

第二章 相关技术研究

【章节规划】
- 建议字数：`2600` 字。
- 章节目标：说明本系统为何选择这些技术栈，以及这些技术分别解决了什么问题。
- 写作原则：只写当前系统实际使用到的技术，不补充未落地技术。

2.1 LangGraph 多智能体编排框架

【写作规划】
- 建议字数：`600` 字。
- 叙述重点：
  1. 解释为什么本系统不是单次提示词调用，而需要工作流编排框架。
  2. 结合 `backend/app/agents/workflow.py` 说明 LangGraph 适合处理状态流转、条件分支、节点重试与多步任务。
  3. 写清楚本文实际节点：`resolve_product_context`、`ensure_product_data`、`router_agent`、`sql_agent`、`rag_agent`、`visual_agent`、`answer_agent`、`master_agent`、`finalize`。
  4. 说明 LangGraph 在本文中承担的是“流程编排层”，不是“模型本体”。
- 引用安排：
  - 如需引用，仅使用上文已分配的“多 Agent 协作理论文献”。
- 配图/表格规划：
  - 可放 1 个“LangGraph 工作流节点职责表”。
  - 不建议在本节重复放完整工作流图，完整流程图更适合放到第三章或第五章。

2.2 FastAPI 后端开发框架

【写作规划】
- 建议字数：`350` 字。
- 叙述重点：
  1. 说明 FastAPI 适合构建 RESTful 接口、支持异步处理和自动文档。
  2. 结合 `backend/app/main.py` 说明本文后端由认证、会话、分析任务三类接口组成。
  3. 强调它在本文中的作用是承载统一消息入口、任务查询和结果查询。
- 写作依据：
  - `backend/app/main.py`
  - `docs/5.API接口定义.md`
- 配图/表格规划：
  - 本节可放 1 个“后端接口分组表”，概括认证、会话、任务三个模块。

2.3 Vue 前端开发框架

【写作规划】
- 建议字数：`350` 字。
- 叙述重点：
  1. 说明 Vue 3 + Pinia + Vue Router 适合构建前端单页工作台。
  2. 结合 `frontend-vue/src/main.ts`、`frontend-vue/src/router/index.ts`、`frontend-vue/src/stores/chat.ts` 说明前端具备会话管理、任务轮询、结果展示能力。
  3. 说明前端不是简单聊天窗口，而是“聊天 + 任务 + 报告展示”的工作台。
- 配图/表格规划：
  - 本节可放 1 个“前端页面与状态管理对应表”。

2.4 SQLAlchemy ORM 和数据库设计

【写作规划】
- 建议字数：`450` 字。
- 叙述重点：
  1. 说明 ORM 的作用是连接业务对象与表结构，降低数据访问复杂度。
  2. 结合 `docs/4.数据库设计.md` 写清核心表：`users`、`products`、`comments`、`conversations`、`messages`、`analysis_tasks`、`analysis_reports`。
  3. 说明任务实体与结果实体分离是本文数据库设计的关键点。
  4. 强调商品数据共享、用户链路数据私有化这一本系统边界。
- 配图/表格规划：
  - 本节可放 1 个“核心数据表作用汇总表”。

2.5 DuckDB 内存分析引擎与 ChromaDB 向量数据库

【写作规划】
- 建议字数：`550` 字。
- 叙述重点：
  1. 解释为什么 MySQL 不直接承担分析引擎和语义检索角色。
  2. 写 DuckDB 在本文中的作用：评论临时聚合、轻量统计分析。
  3. 写 ChromaDB 在本文中的作用：评论向量索引与语义召回。
  4. 结合 `backend/app/services/vector_store_service.py` 写清“主数据在 MySQL，检索能力在 ChromaDB”的边界。
  5. 如需补一句 LLM 侧能力，可说明嵌入由远程 Embedding 接口提供，但不要虚构模型细节。
- 引用安排：
  - 如需引用，仅使用上文已分配的“RAG / 检索增强文献”。
- 配图/表格规划：
  - 可放 1 个“存储层分工对比表”。
  - 表头建议包含：存储组件、数据类型、在本文中的角色、是否保存业务真相、主要服务对象。

2.6 本章小结

【写作规划】
- 建议字数：`300` 字。
- 叙述方式：
  - 总结技术栈分工：LangGraph 负责任务编排，FastAPI 负责接口，Vue 负责交互，MySQL 负责业务真相，DuckDB 和 ChromaDB 负责分析辅助。
  - 为第三章系统设计过渡。

第三章 系统概要设计

【章节规划】
- 建议字数：`3000` 字。
- 章节目标：从整体到局部说明系统怎么被设计成一个可运行的电商分析智能体。
- 写作原则：设计必须严格对应现有架构，不可写不存在的模块。

3.1 系统整体架构设计

【写作规划】
- 建议字数：`800` 字。
- 叙述重点：
  1. 使用 `docs/2.系统整体架构图.md` 的分层结构写整体架构。
  2. 明确六层：前端展示层、API 接口层、服务层、工作流层、数据采集与分析层、数据存储层。
  3. 突出“统一消息入口 + 任务状态推进 + 结构化结果返回”的核心设计思想。
  4. 建议在正文插入一张总架构图。
- 引用安排：
  - 如需引用，仅使用上文已分配的“分层式 Agent 架构文献”。
- 配图/表格规划：
  - 建议必须放 1 张“系统总体架构图”。
  - 图源可直接整理自 `docs/2.系统整体架构图.md` 的分层结构。

3.2 三层存储架构设计（MySQL / DuckDB / ChromaDB）

【写作规划】
- 建议字数：`650` 字。
- 叙述重点：
  1. 说明 MySQL 存业务真相，DuckDB 做分析聚合，ChromaDB 做评论向量检索。
  2. 说明为什么商品评论按商品复用，不按用户重复存储。
  3. 说明 `analysis_tasks` 与 `analysis_reports` 的设计意义。
  4. 建议插入一张数据流向图或 E-R 图前置说明。
- 写作依据：
  - `docs/4.数据库设计.md`
  - `docs/2.系统整体架构图.md`
- 配图/表格规划：
  - 建议放 1 张“三层存储架构图”。
  - 建议再放 1 个“存储层职责表”，与文字说明形成互证。

3.3 多智能体协作架构设计

【写作规划】
- 建议字数：`850` 字。
- 叙述重点：
  1. 说明为什么采用多 Agent，而不是单模型一次性输出。
  2. 按功能拆解各 Agent 的职责，并强调 `router_agent`、`sql_agent`、`rag_agent`、`visual_agent`、`answer_agent`、`master_agent` 的分工。
  3. 重点描述前置节点 `resolve_product_context` 和 `ensure_product_data` 的价值，它们是本文工作流区别于普通问答系统的关键。
  4. 说明 `master_agent` 的审查与重试机制如何提升结果稳定性。
- 本节原则上不单独安排参考文献，重点依托当前代码中的节点职责和工作流结构展开。
- 配图/表格规划：
  - 建议必须放 1 张“多 Agent 协作流程图”。
  - 建议补 1 个“Agent 职责与输入输出表”。
  - 表头建议包含：Agent/节点名称、输入、输出、在工作流中的作用。

3.4 数据库设计与 E-R 图

【写作规划】
- 建议字数：`450` 字。
- 叙述重点：
  1. 逐类介绍实体：用户、商品、评论、对话、消息、任务、报告。
  2. 说明共享数据与私有链路数据的边界。
  3. 指出一条消息触发一个任务、一个任务产出一份报告的因果关系。
  4. 这一节可与 `docs/4.数据库设计.md` 的 E-R 图保持一致。
- 配图/表格规划：
  - 建议必须放 1 张 E-R 图。
  - 可补 1 个“核心实体关系说明表”，帮助解释主外键关系。

3.5 本章小结

【写作规划】
- 建议字数：`250` 字。
- 叙述方式：总结“系统设计如何为后续实现提供结构支撑”，并引出第四章需求分析。

第四章 系统需求分析

【章节规划】
- 建议字数：`2700` 字。
- 章节目标：明确系统解决什么问题、服务谁、提供什么能力、需要达到什么运行要求。
- 写作提醒：虽然本章在目录上晚于概要设计，但内容仍然要按标准需求分析逻辑书写。

4.1 系统描述

【写作规划】
- 建议字数：`450` 字。
- 叙述重点：
  1. 概述系统定位：面向普通消费者的商品分析辅助系统。
  2. 说明用户输入形式：自然语言 + 商品链接。
  3. 说明系统输出形式：任务进度、文本结论、图表、证据评论。
  4. 明确系统当前支持京东商品评论分析。

4.2 功能性需求分析

【写作规划】
- 建议字数：`900` 字。
- 建议拆成五类需求写：
  1. 用户认证与会话管理需求。
  2. 商品上下文识别与消息接入需求。
  3. 评论数据采集与准备需求。
  4. 多 Agent 分析与结果生成需求。
  5. 前端展示与任务轮询需求。
- 每类需求都要写“输入、处理、输出”。
- 必须明确：
  - 邮件推送是扩展功能，当前未实现。
  - 当前系统的真实消息入口是统一接口，不写成“普通聊天”和“商品分析”两套并行入口。
- 本节原则上不单独安排参考文献，需求分析优先依据系统目标、输入输出和现有功能边界展开。

4.3 非功能性需求分析

【写作规划】
- 建议字数：`500` 字。
- 叙述重点：
  1. 易用性：自然语言交互、可视化结果展示。
  2. 可维护性：前后端分离、模块化服务、多 Agent 节点职责清晰。
  3. 可扩展性：新平台爬虫、新分析节点、新图表模板可继续扩展。
  4. 安全性与可靠性：鉴权、任务状态持久化、异常回写、失败重试。
- 本节原则上不单独安排参考文献，非功能需求以当前系统架构和工程约束为主。

4.4 可行性分析

【写作规划】
- 建议字数：`500` 字。
- 建议分三部分写：
  1. 技术可行性：现有栈已能支撑采集、分析、检索、前后端闭环。
  2. 业务可行性：商品评论分析有明确使用场景和决策价值。
  3. 实现可行性：项目已有真实代码、接口、数据库和前端页面，不是停留在概念验证。
- 引用安排：
  - 如需引用，仅使用上文已分配的“数据分析智能体可行性文献”。

4.5 用户用例分析

【写作规划】
- 建议字数：`350` 字。
- 建议写一个核心主用例即可：
  - 用户登录系统。
  - 创建或进入会话。
  - 发送问题和商品链接。
  - 系统创建任务并展示进度。
  - 用户查看文本结论、图表和证据。
- 可以补一个异常用例：
  - 商品数据不足或抓取失败时，系统返回失败状态和提示信息。
- 配图/表格规划：
  - 建议放 1 张“用户用例图”或 1 个“核心用例说明表”。

4.6 本章小结

【写作规划】
- 建议字数：`250` 字。
- 叙述方式：总结需求如何对应第三章的设计和第五章的实现。

第五章 核心模块实现

【章节规划】
- 建议字数：`3500` 字。
- 章节目标：按照代码真实结构，写清系统是如何从输入消息走到分析结果的。
- 写作原则：这一章最需要“代码对应性”，必须多引用现有模块职责，少写空泛原理。

5.1 数据采集与清洗模块（DrissionPage 爬虫）

【写作规划】
- 建议字数：`700` 字。
- 叙述重点：
  1. 说明 `backend/app/crawlers/jd_crawler.py` 基于 DrissionPage 打开商品页和评论弹层。
  2. 说明其关键流程：标准化商品链接、进入商品页、监听评论接口、滚动评论区域、解析 `commentInfo`、去重、清洗。
  3. 说明采集结果如何写入 `comments` 表，并进一步触发向量化。
  4. 强调这是真实京东评论抓取，不是静态样例数据。
- 引用安排：
  - 如需引用，仅使用上文已分配的“在线评论服务评价文献”。
- 配图/表格规划：
  - 建议放 1 张“评论采集与清洗流程图”。
  - 如已截取真实运行页面，可补 1 张“京东商品页/评论页抓取过程截图”，但必须是可公开、安全的运行截图。

5.2 多智能体工作流实现（LangGraph 状态机编排）

【写作规划】
- 建议字数：`900` 字。
- 叙述重点：
  1. 结合 `AnalysisService.process_task()` 说明任务如何被送入 `AnalysisWorkflow.run()`。
  2. 结合 `workflow.py` 写出节点与边的关系。
  3. 重点讲三件事：
     - 前置商品上下文解析。
     - 数据准备与按需爬取。
     - 路由、执行、审查、收敛的闭环。
  4. 说明 `master_agent` 的重试决策机制是保证质量的重要设计。
- 引用安排：
  - 如需引用，仅使用上文已分配的“分析型智能体链路文献”。
- 配图/表格规划：
  - 建议必须放 1 张“LangGraph 状态流转图”。
  - 可补 1 个“任务阶段与进度推进表”，对应 `resolve_product_context` 到 `finalize`。

5.3 数据分析与检索模块（SQL 聚合 + 向量检索）

【写作规划】
- 建议字数：`700` 字。
- 叙述重点：
  1. 说明 SQL Agent 负责结构化统计，如差评率、评分分布、维度分布等。
  2. 说明 RAG Agent 负责从评论语料中召回证据，给出具体评论片段和洞察。
  3. 说明可视化 Agent 将结构化统计转成图表配置。
  4. 强调“SQL 负责可量化指标，RAG 负责语义证据，二者共同支撑最终答案”。
- 引用安排：
  - 如需引用，仅使用上文已分配的“评论语义理解文献”。
- 配图/表格规划：
  - 建议放 1 张“分析与检索模块数据流图”。
  - 建议补 1 个“SQL 分析结果与 RAG 证据结果对照表”。
  - 如系统已有真实图表输出，可补 1 张“分析结果图表示例截图”。

5.4 后端 API 设计与实现（FastAPI 路由）

【写作规划】
- 建议字数：`500` 字。
- 叙述重点：
  1. 按 `docs/5.API接口定义.md` 写接口分组：认证模块、会话模块、分析任务模块。
  2. 强调核心接口：
     - `POST /api/conversations/{id}/messages`
     - `GET /api/analysis/tasks/{task_id}`
     - `GET /api/analysis/tasks/{task_id}/result`
     - `POST /api/analysis/tasks/{task_id}/retry`
  3. 说明后端协议已经从“同步问答”演进为“任务化分析协议”。
- 配图/表格规划：
  - 建议放 1 个“核心 API 汇总表”。
  - 建议再放 1 张“接口交互时序图”，展示发送消息、创建任务、轮询进度、获取结果的三段式过程。

5.5 前端交互界面设计（Vue 前端构建）

【写作规划】
- 建议字数：`450` 字。
- 叙述重点：
  1. 说明前端采用 Vue 单页工作台结构，核心页面为登录工作区和聊天工作台。
  2. 结合 `frontend-vue/src/stores/chat.ts` 说明前端如何管理会话、任务缓存和轮询。
  3. 说明前端展示内容不是单条字符串回复，而是消息流 + 任务状态 + 分析结果组件。
  4. 可用一张界面截图支撑。
- 配图/表格规划：
  - 建议放 2-3 张系统截图：
    - 登录页截图
    - 聊天工作台截图
    - 分析结果展示截图
  - 如版面有限，至少保留“聊天工作台 + 分析结果”两张关键截图。

5.6 本章小结

【写作规划】
- 建议字数：`250` 字。
- 叙述方式：总结“采集、分析、接口、前端”四部分如何形成完整闭环。

第六章 系统测试

【章节规划】
- 建议字数：`1900` 字。
- 章节目标：说明系统是否能稳定运行、功能是否闭环、结果是否可用，以及当前局限是什么。
- 写作原则：测试结论必须来自真实运行记录、真实接口返回、真实前端页面或真实测试脚本。

6.1 测试环境

【写作规划】
- 建议字数：`300` 字。
- 叙述重点：
  1. 交代后端环境、数据库环境、前端环境、浏览器环境。
  2. 简述关键依赖：FastAPI、Vue、MySQL、DuckDB、ChromaDB、DrissionPage。
  3. 如果有本地 Windows 开发环境，应如实写明。
- 配图/表格规划：
  - 建议放 1 个“测试环境配置表”。
  - 表头建议包含：类别、环境/版本、用途。

6.2 功能测试结果

【写作规划】
- 建议字数：`700` 字。
- 建议围绕真实主链路写测试：
  1. 用户注册登录测试。
  2. 会话创建、切换、删除测试。
  3. 商品链接识别与任务创建测试。
  4. 评论抓取、任务进度推进测试。
  5. 结果查询、图表显示和证据展示测试。
  6. 失败任务重试测试。
- 可结合现有后端测试脚本和接口返回截图展开。
- 配图/表格规划：
  - 建议必须放 1 个“功能测试结果表”。
  - 建议补 1-2 张测试支撑截图，如任务进度页、分析结果页、接口返回结果示例。

6.3 模型测试对比

【写作规划】
- 建议字数：`500` 字。
- 这一节必须谨慎写，建议采用以下原则：
  1. 如果已经做过真实实验，可写“不同模型或不同分析策略”的对比，例如不同大模型输出质量、是否启用 RAG 的结果差异、不同提示策略下的回答覆盖度差异。
  2. 如果当前没有完整实测数据，不得伪造准确率、召回率、人工评分等数字。
  3. 在暂无真实对比数据时，本节可改写为“模型测试设计与评价指标规划”，重点写：
     - 评价维度：完整性、准确性、证据一致性、图表匹配度、响应稳定性。
     - 评价方法：人工评阅 + 典型案例比对。
- 引用安排：
  - 如需引用，仅使用上文已分配的“大语言模型评估文献”。
- 配图/表格规划：
  - 建议放 1 个“模型/策略对比评价表”。
  - 如果后续补齐真实实验数据，可增加 1 张柱状图或雷达图展示评分对比；若无真实数据，则不放统计图，只保留规划表。

6.4 系统不足与改进方向

【写作规划】
- 建议字数：`250` 字。
- 必须真实写出当前不足：
  1. 商品平台支持范围有限，当前以京东为主。
  2. 邮件推送等扩展功能尚未落地。
  3. 模型评估与对比实验仍需进一步完善。
  4. 爬虫稳定性和平台反爬策略可能影响数据获取。
- 本节原则上不单独安排参考文献，不足与改进方向应以当前系统真实边界和测试结果为主。
- 配图/表格规划：
  - 本节可不放图。
  - 如需增强概括性，可放 1 个“不足与改进方向对应表”。

6.5 本章小结

【写作规划】
- 建议字数：`150` 字。
- 叙述方式：总结系统已具备的能力边界与后续改进方向。

结论

【写作规划】
- 建议字数：`500` 字。
- 建议按“完成了什么、验证了什么、还缺什么”三层收束：
  1. 概括本文构建了一个面向电商商品评论分析的 LLM 智能体系统。
  2. 概括系统实现了统一消息入口、任务化分析、多 Agent 协作、真实评论抓取、结构化结果展示。
  3. 概括论文的主要贡献：工程实现闭环、架构整合、面向商品决策场景的落地价值。
  4. 最后简述不足与未来工作：更多平台接入、更严格模型评估、更强鲁棒性和更完善的报告服务。

参考文献

【规划提示】
- 建议按学校要求的参考文献格式统一整理，优先使用 GB/T 7714。
- 本文可优先纳入的 11 篇文献已经在各章完成了引用位置规划，正式写作时应按章节需要展开为正文引用。

致谢

【规划提示】
- 致谢不宜过长，建议 `300-500` 字。
- 主要感谢导师、项目支持人员、测试与使用反馈提供者，不写夸张表述。
