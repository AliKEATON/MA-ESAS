# 项目进度 - 2026-04-12

## 1. 项目当前状态

当前项目已经完成了“需求文档统一”和“后端核心链路打通”的第一阶段工作，整体处于：

- 文档口径已统一
- 后端基础数据模型已对齐
- 统一消息入口已可用
- 分析任务已支持创建、执行、查询、失败、重试
- Streamlit 前端尚未同步这些新能力
- 真正的多智能体分析能力尚未接入

当前更准确的判断是：

- 项目已经不是“只有静态设计文档”的阶段
- 项目已经进入“后端 MVP 可联调、前端待接入、AI 分析待增强”的阶段

---

## 2. 已完成内容

### 2.1 文档体系已统一

已完成以下文档修订：

- `docs/0.文档统一修订方案.md`
- `docs/2.系统整体架构图.md`
- `docs/3.项目目录结构.md`
- `docs/4.数据库设计.md`
- `docs/5.API接口定义.md`

统一后的核心口径：

- 前端基线统一为 `frontend-streamlit/`
- 前端统一消息入口，后端自动分流
- 商品数据定义为共享数据
- 对话、消息、分析任务、分析报告定义为用户私有数据
- 区分内部主键 `products.id` 与平台商品 ID `external_product_id`
- `analysis_tasks` 作为异步任务持久化实体

### 2.2 数据库与模型已对齐

已完成数据库模块第一阶段调整：

- 新增 `analysis_tasks` 模型与迁移
- 对齐 `products/comments/conversations/messages/analysis_reports`
- 补上商品共享模型与任务模型需要的字段

当前数据库设计关键点：

- `products` 去掉 `user_id`
- `products` 新增 `external_product_id`
- `conversations.product_id` 改为 `bound_product_id`
- `messages` 新增 `message_type`
- `analysis_reports` 与 `analysis_tasks` 建立关系

### 2.3 后端接口基础链路已打通

当前已存在并可继续扩展的接口模块：

- `backend/app/api/auth.py`
- `backend/app/api/conversations.py`
- `backend/app/api/analysis.py`
- `backend/app/api/crawlers.py`

已经实现的核心能力：

- 用户认证基础接口
- 会话创建
- 会话列表
- 会话详情
- 会话消息列表
- 统一消息发送入口
- 分析任务进度查询
- 分析任务结果查询
- 失败任务重试
- 会话关联任务列表查询

### 2.4 消息入口已支持两条后端处理链路

当前 `POST /api/conversations/{conversation_id}/messages` 已支持：

#### 普通消息链路

- 保存用户消息
- 生成一条占位 `assistant` 回复
- 返回 `handling_mode = direct_reply`

#### 商品分析链路

- 识别商品链接
- 查找或创建商品
- 创建 `analysis_task`
- 保存任务创建通知消息
- 返回 `handling_mode = task_created`
- 自动挂入后台执行

### 2.5 分析任务已具备基础执行闭环

当前分析任务已经支持：

- 任务创建
- 后台执行
- 状态推进
- 进度查询
- 结果查询
- 失败记录
- 失败消息写回
- 失败任务重试

当前任务执行步骤为基础版：

- `link_extract`
- `crawl_check`
- `crawling`
- `sql_agent`
- `rag_agent`
- `synthesizer`

说明：

- 这几个步骤目前是“基础执行框架 + 基础统计报告”
- 还不是最终的 LangGraph 多智能体正式分析版本

### 2.6 会话消息流已能体现任务结果

当前会话流中已经能看到：

- 用户原始问题
- 任务创建通知消息
- 普通聊天回复消息
- 分析完成结果消息
- 分析失败通知消息

因此现在前端即使先不接专门的任务页，也能通过会话消息感知任务结果。

### 2.7 日志可观测性已增强

当前关键链路已经加入日志打印，便于排查问题：

- 消息分流
- 任务创建
- 任务调度
- 任务状态推进
- 爬虫是否触发
- 统计是否生成
- 报告是否写入
- 结果消息是否写回
- 失败原因
- 重试调度

---

## 3. 当前已落地的后端能力总结

从“用户实际可验证”的角度，后端目前已经具备：

1. 用户可以创建会话
2. 用户可以发送普通消息并得到 assistant 占位回复
3. 用户可以发送商品分析消息并创建任务
4. 用户可以查询任务进度
5. 用户可以查询任务结果
6. 用户可以在会话详情里看到分析完成或失败消息
7. 用户可以对失败任务发起重试
8. 用户可以查看某个会话下的任务列表

换句话说，后端 MVP 的“主业务骨架”已经成形。

---

## 4. 当前未完成/待完善内容

虽然主链路已经打通，但当前仍有明显未完成项。

### 4.1 前端尚未接入新后端能力

当前 `frontend-streamlit/` 尚未完成以下同步：

- 统一消息入口的两种返回处理
- 任务进度轮询
- 会话任务列表展示
- 结果消息渲染
- 失败任务重试按钮

### 4.2 普通聊天仍是占位回复

当前普通聊天不是调用真正的大模型，而是：

- 保存用户消息
- 返回一条占位 assistant 消息

这意味着当前系统的“普通对话能力”还不算真正完成。

### 4.3 分析任务仍是基础版执行

当前分析任务虽然能运行，但本质上还是：

- 链接识别
- 爬虫检查/触发
- 评论统计
- 基础 summary
- 基础 evidence

尚未完成：

- Router Agent
- SQL Agent 真正 DuckDB 分析
- RAG Agent 真正语义检索
- Synthesizer 真正 LLM 结果生成

### 4.4 报告能力仍是基础版

当前报告已能落库，但仍未完善：

- 邮件发送完整闭环
- 更丰富的图表配置
- 更结构化的结论
- 多维度评论洞察
- 可下载报告导出

### 4.5 一些工程问题仍待处理

当前还存在这些工程层问题：

- 仓库里存在部分历史中文乱码注释
- `config.py` 中有明显编码异常文本
- 尚未完成系统级联调验证
- 尚未补测试
- 尚未验证完整数据库迁移在真实环境中的表现

---

## 5. 当前建议的项目阶段判断

当前项目可定义为：

**阶段：后端 MVP 已成型，进入前后端联调与 AI 能力增强阶段**

如果以论文/毕业设计节奏来判断，目前最合理的推进方式不是继续大范围改架构，而是：

- 先把当前后端能力稳定住
- 再把 Streamlit 前端接进来
- 最后逐步增强 AI 分析质量

---

## 6. 后续任务规划

以下任务按照优先级排序。

### P0：必须优先完成

#### 任务 1：前端接入统一消息入口

目标：

- Streamlit 前端调用新的 `/api/conversations/{id}/messages`
- 正确处理 `direct_reply` 和 `task_created`

完成后可验证：

- 普通消息立即展示回复
- 分析消息创建任务并启动后续轮询

#### 任务 2：前端接入任务进度轮询

目标：

- 调用 `/api/analysis/tasks/{task_id}`
- 展示 `pending/processing/completed/failed`
- 展示步骤与进度

完成后可验证：

- 用户能实时看到分析任务执行过程

#### 任务 3：前端展示分析结果与失败信息

目标：

- 展示 `analysis_result` 消息
- 展示失败通知消息
- 支持查看分析详情

完成后可验证：

- 用户在界面中能完整看到任务成功/失败结果

#### 任务 4：前端接入失败任务重试

目标：

- 调用 `/api/analysis/tasks/{task_id}/retry`
- 失败后可以重新执行任务

完成后可验证：

- 失败任务可由用户手动恢复

---

### P1：重要但可排在前端联调后

#### 任务 5：真实普通聊天能力接入

目标：

- 将占位 assistant 回复替换为真实 LLM 回复

建议实现：

- 增加独立聊天服务模块
- 抽象 chat provider
- 先接 DeepSeek 或已有可用模型

#### 任务 6：分析任务真正接入多智能体能力

目标：

- 用真实的 Agent 替换当前基础分析逻辑

建议拆分：

- Router Agent：判断分析意图/任务类型
- SQL Agent：基于 DuckDB 做评论统计分析
- RAG Agent：基于 ChromaDB 做相似评论检索
- Synthesizer：生成最终自然语言分析结果

#### 任务 7：分析报告增强

目标：

- 让 `analysis_report` 从“基础统计结果”升级成“正式分析报告”

建议增强：

- 情感分布
- 好评/差评主题
- 多维度产品分析
- 图表配置增强
- 结构化洞察输出

---

### P2：工程质量与交付完善

#### 任务 8：补充接口联调与集成测试

建议覆盖：

- 会话接口
- 消息接口
- 任务进度接口
- 任务结果接口
- 重试接口
- 会话任务列表接口

#### 任务 9：修正历史编码与注释问题

目标：

- 逐步清理乱码注释
- 提升代码可维护性

优先处理：

- `config.py`
- 路由层中文注释
- 服务层历史遗留中文乱码

#### 任务 10：补邮件发送闭环

目标：

- 用户可以在分析完成后触发报告发送

需要补齐：

- 邮件服务
- 邮件接口
- 发送记录
- 前端触发入口

---

## 7. 推荐的最近执行顺序

建议下一阶段严格按下面顺序推进：

1. Streamlit 前端接统一消息入口
2. Streamlit 前端接任务进度轮询
3. Streamlit 前端接结果展示与失败展示
4. Streamlit 前端接失败重试
5. 普通聊天接真实 LLM
6. 分析任务接真实多智能体流程
7. 增强报告与邮件发送
8. 补测试与清理工程质量问题

这是当前最稳妥的推进顺序，因为它遵循：

- 先把已有后端能力真正用起来
- 再增强 AI 质量
- 最后补工程完备性

---

## 8. 当前结论

截至 **2026-04-12**，项目已经完成了从“设计分散、概念冲突”到“后端核心骨架可运行”的关键跃迁。

当前最重要的不是继续扩写文档，也不是继续大改数据库，而是：

- 让前端尽快接入
- 让用户能在界面里完整体验当前后端能力
- 再逐步把 AI 分析链路从基础版替换成正式版

如果按毕业设计推进节奏判断，当前项目已经具备继续进入“系统联调与功能完善阶段”的条件。
