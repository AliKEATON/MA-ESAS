# --- Web Frameworks (前后端) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
streamlit>=1.32.0
streamlit-echarts>=0.4.0
jinja2>=3.1.3
python-multipart>=0.0.6   # 文件上传支持

# --- AI & Agent Orchestration (核心大脑) ---
# LangChain 体系 (确保版本兼容)
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.7   # 用于调用 DeepSeek (兼容 OpenAI 协议)
langgraph>=0.0.50         # 确保支持 StateGraph 最新语法

# --- Crawler (爬虫) ---
DrissionPage>=4.0.4       # 浏览器自动化，建议使用 4.x 稳定版
tenacity>=8.2.0           # 重试机制（网络超时、反爬虫）

# --- Database & Vector Store (数据存储) ---
sqlalchemy>=2.0.29        # ORM 框架，v2 版本
pymysql>=1.1.0            # MySQL 驱动
chromadb>=0.5.0           # 向量数据库
duckdb>=0.9.0             # 内存分析引擎
sqlalchemy-utils>=0.41.0  # SQLAlchemy 扩展工具
alembic>=1.13.0           # 数据库迁移工具

# --- Authentication & Security (认证与授权) ---
python-jose[cryptography]>=3.3.0  # JWT 认证
passlib[bcrypt]>=1.7.4            # 密码加密
bcrypt>=4.1.0                     # bcrypt 显式指定版本
email-validator>=2.1.0            # 邮箱验证

# --- Email & Report Generation (邮件与报告) ---
aiosmtplib>=3.0.0         # 异步邮件发送
weasyprint>=60.0          # PDF 生成

# --- Data Processing (数据清洗) ---
pandas>=2.2.0
numpy>=1.26.0

# --- Utilities (工具) ---
pydantic>=2.7.0           # 数据验证 (FastAPI 依赖此版本)
python-dotenv>=1.0.1      # 读取 .env 文件
requests>=2.31.0          # 基础 HTTP 请求
loguru>=0.7.0             # 日志记录
